# 01: Foundation of the model and the validator

**What to build:** the repository, the four parts of the model (requirements, verification cases, architecture, evidence register), the assumption register, the validation rules, the generated traceability matrix, the test suite, the CI pipeline and the README — enough that a reader can open the repository and check the study in two commands.

**Blocked by:** None (can start immediately).

**Status:** resolved

- [x] Doorstop documents `model/requirements` (SYS) and `model/verification` (VER, linked to SYS)
- [x] Architecture model with functions, components, interfaces and externals
- [x] Evidence register with one record per verification case; every record `FUTURE` with a plan
- [x] Assumption register with statuses and rationales
- [x] One validation rule per failure mode, each with a stable identifier
- [x] `python -m tools.traceability check` green on the repository model
- [x] `docs/traceability.md` generated by the tool and committed
- [x] Unit test per rule, integration tests on the repository model, CLI tests
- [x] CI runs Doorstop, the validator, the freshness check and the tests
- [x] README with what the repository is, what it is not, and the status of the evidence

## Answer

Built on 2026-09-18. 23 requirements (SYS001–SYS023), 16 verification cases (VER001–VER016),
11 components, 9 functions, 8 interfaces, 11 declared assumptions. The validator reports
0 errors and 0 warnings; the suite is green; `docs/traceability.md` is generated by
`python -m tools.traceability report`. Every verification case is `FUTURE` and points at the
ticket that will produce its evidence.
