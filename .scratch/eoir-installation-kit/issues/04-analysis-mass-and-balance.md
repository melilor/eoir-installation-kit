# 04: Mass and balance analysis

**What to build:** the analysis that produces the kit mass budget and the platform weight and balance impact, as a script with its output committed as the artifact of the verification cases it closes.

**Blocked by:** 01, 02.

**Status:** ready-for-agent

- [ ] Component masses are declared per element and summarised by the script, not typed into a table
- [ ] Kit mass compared with the SYS005 target, with the margin stated
- [ ] Platform centre of gravity computed for the loadable configurations, compared with the SYS007 range
- [ ] Payload envelope check (SYS006) using the declared interface data
- [ ] Output committed under `evidence/` and referenced by the artifact field of VER003 and VER004
- [ ] The three verification cases move out of `FUTURE` with the status the result supports
